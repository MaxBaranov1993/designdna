# Design Studio v3 — первоклассный дизайн в ноде «Генератор»

Дата: 2026-09-03. Аудит качества генерации и архитектура решения. Ориентир — как устроен Claude Design (Design Components: модель пишет настоящий HTML/CSS с инлайн-стилями, flex/grid, Google Fonts, oklch-палитра; ремесло держат правила «settle the aesthetic», «bold direction», запрет AI-слопа, «copy is the product», варианты как артборды).

## 1. Что генерируется сейчас (факты)

Сэмпл: seller-страница rsale (`data/rsale-e2e-profile`, генератор node 5, `results/gen_sample_rsale_full.png`).

- Страница читается как **wireframe с брендовыми цветами**: серый градиент-заглушка в hero, две одинаковые карточки «24/7 · 0 ₽», секция «Три шага» с пустым «Шаг 2», футер сплошной фиолетовой плитой.
- Токены плоские: `surface == background` (белое на белом, ритма нет), `textMuted #2b2b2b` (почти чёрный), **body Manrope 700** (весь текст жирный — дефект извлечения ДС).
- Типографика: фиксированные `calc(17px*var(--fs))`, заголовки маленькие, нет шкалы ролей.
- Композиция: `hero split → feature-grid → steps → cta → footer` — одна и та же скелетная последовательность для любого продукта; `designkb.design_direction` меняет только палитру по кругу, не направление.

## 2. Почему (корневые причины)

| # | Причина | Где |
|---|---|---|
| 1 | **Закрытая библиотека из 16 блоков × 2–3 варианта, отрисованных строковыми шаблонами** с фиксированными отступами. Модель выбирает из меню, а не компонует. Потолок качества = потолок шаблонов. | `app/prompts/BLOCKS.md`, `engine/renderer.ts` (`if (t === "hero")…`) |
| 2 | **Токены слишком грубые**: 8 цветов, 2 шрифта, скаляры radius/spacing/shadow. Нет тональной палитры, шкалы типографики по ролям, elevation. | `schema/design-ir.schema.json` tokens |
| 3 | **Промпт противоречив**: «You are an EDITING tool first» доминирует над craft-правилами; `DESIGN.md` хорош, но не подкреплён примерами (few-shot нет) и не проверяется. | `spike/system-prompt.md`, `app/prompts/DESIGN.md` |
| 4 | **Нет шага арт-дирекции**: направление, тон, типографская пара, палитра и план ритма не выбираются явно; `design_direction` = палитра по кругу + UX-правила. | `app/designkb.py` |
| 5 | **Одношаговая генерация без взгляда на пиксели**: `qualitygate` — детерминированные правила (контраст, сетка, overflow); судья Quality Pass смотрит JSON, а не рендер. | `app/qualitygate.py`, `_quality_scorecard` |
| 6 | `frame`/примитивы (heading/text/button/image/rect/frame с auto-layout) уже есть в IR и рендерере, но промпт говорит «Omit `frame` for ordinary flow pages» — свободная композиция выключена намеренно. | `system-prompt.md` § Geometry |

## 3. Целевая архитектура

Принцип: **сохранить Design IR как редактируемое ядро** (это конкурентное преимущество перед Claude Design, где результат — HTML), но дать модели композиционную свободу и замкнуть цикл через пиксели.

### 3.1 Пайплайн `generate` в три стадии

1. **Art direction** (быстрая модель, ~2 с). Вход: бриф, тип продукта, Style DNA/ДС если есть. Выход `DesignBrief` (JSON, схема `schema/design-brief.schema.json`): аудитория и задача; `tone` из именованных направлений (editorial, brutalist-minimal, soft-pastel, luxury, playful, industrial, retro-futuristic, organic…); `typePair` из расширенного курируемого списка характерных пар (display + body, с fallback-стеками); `palette` — oklch-семя (тон фона, 1–2 акцента с одинаковыми chroma/lightness), из которого сервер детерминированно выводит тональную палитру; `rhythm` — план секций с чередованием плотности и **одним** осознанным риском; `copyDeck` — тезис hero, один CTA, доказательства.
2. **Composition → IR** (сильная модель). Промпт переписывается: режим generate становится основным; в контексте — `DesignBrief`, 2–3 **эталонных IR** (few-shot из `app/exemplars/*.json`, ручной работы, по типам продуктов), правило «секции компонуются из примитивов и auto-layout-фреймов; семантические блоки — ускорители, а не потолок». Вариантов ≥2 — **разные направления**, не разные палитры; у каждого подпись направления.
3. **Pixel critique** (vision-судья). Сервер рендерит IR в PNG (офлайн-путь уже есть: `timeline_render.py` / headless Chromium), судья получает скриншот + рубрику (иерархия, ритм, плотность, типографика, цвет, слоп-тропы, соответствие брифу) → score 0–100 и адресные правки → repair → повторный судья (кооперативная отмена и стадии через `run_registry` уже есть). Порог по умолчанию 80; ниже — нода показывает «нужна доработка» с причинами.

### 3.2 Токены v2

`tokens.v2` (обратно совместимо, миграция в `ir/migrate.py`):
- `color`: `bg`, `bg2`, `surface`, `surface2`, `ink`, `ink2`, `inkMuted`, `line`, `accent`, `accentInk`, `accent2` — генерируются из oklch-семени серверно (`app/colorutils.py`), с проверкой WCAG для всех пар «текст/фон».
- `type`: роли `display/h1/h2/h3/lead/body/small/eyebrow` с `size/lineHeight/tracking/weight`; `families.display/body` + fallback-стек; шкала считается из `base` и `ratio`.
- `space`: шкала 4…160; `radius`: `sm/md/lg/pill`; `shadow`: `sm/md/lg`; `motion`: одна reveal-анимация.
- Рендерер читает роли, а не `calc(17px*var(--fs))`.

### 3.3 Свободная композиция в рендерере

- Секция может быть `type: "composition"`: дерево примитивов (`frame` auto-layout/free, `heading`, `text`, `button`, `image`, `rect`, `divider`) — рендерер это уже умеет для `frame`; нужно снять ограничения промпта и добавить `composition` как первоклассный блок в схему, BLOCKS.md и инспектор.
- Семантические блоки получают **новые варианты с характером** (hero: `editorial-stack`, `poster`, `split-offset`, `numbered`; feature: `list-rail`, `bento-asym`, `two-col-manifest`), фон секций — из `bg/bg2/surface`, не только чередование.
- Заглушки изображений: остаются (пользователь заменяет своим файлом — уже реализовано), но получают арт-дирекцию из `copyDeck.imagery` и **тон заглушки из палитры**, а не серый градиент.

### 3.4 Выбор направления в ноде (как «settle the aesthetic»)

- Генератор показывает 3 чипа направлений (из стадии 1) с одной строкой мотивации и компромисса; пользователь выбирает или «сразу все три»; выбранное направление запоминается в taste-профиле проекта.
- Итог — варианты подписаны направлением, а не «Вариант 1/2».

### 3.5 Эталоны и рубрика

- `app/exemplars/`: 10–15 IR-страниц ручной работы по типам продуктов (лендинг SaaS, маркетплейс, ресторан, портфолио, финтех, e-commerce…), каждая проходит vision-судью на ≥90. Используются как few-shot и как якоря для судьи.
- `app/prompts/RUBRIC.md`: рубрика судьи с примерами «плохо/хорошо».

## 4. Разбиение на задачи (для оркестрации)

| ID | Задача | Владелец | Зависит |
|---|---|---|---|
| T1 | Схема `DesignBrief` + стадия арт-дирекции (`app/art_direction.py`, роутинг на быструю модель, кэш по брифу), расширенный список типографских пар, oklch-палитра в `colorutils.py` с WCAG-проверкой | Codex sol medium | — |
| T2 | Токены v2: схема, миграция, рендерер по ролям, инспектор/DS-панель читают новые роли | Opus | T1 (контракт токенов) |
| T3 | Блок `composition` + новые варианты блоков + переписанный system-prompt (generate-first, few-shot, правило композиции) + 3 первых эталона | Opus | T1 |
| T4 | Vision-судья: рендер IR → PNG на сервере, рубрика, `quality_pass` с изображением, порог в ноде, стадии через `run_registry` | Codex sol medium | — |
| T5 | UI ноды: чипы направлений, подписи вариантов, статус «нужна доработка» с причинами | Codex sol medium | T1, T4 |
| T6 | Библиотека эталонов до 10+ и прогон всех через судью, отчёт качества до/после на 5 брифах | Codex sol medium | T3, T4 |

### Статус (2026-09-03)

| ID | Статус | Коммит | Исполнитель |
|---|---|---|---|
| T1 | слит | 33986ba | Codex sol medium (Orca) |
| T4 | слит | aa87e59 | Codex sol medium (Orca) |
| T2 | слит | 155da0c | Opus (субагент координатора, Orca-логин Claude недоступен) |
| T3 | слит | 22de04d | Opus (субагент координатора) |
| T5 | слит | a7cded4 | Codex sol medium (Orca) |
| T6 | слит | 6ddb3ae | Codex sol medium (Orca) |

Итог слияния (ветка `codex/unified-motion-design`): pytest 491, svelte-check и сборка чистые, UI-тесты новых частей и регрессия движка зелёные. Библиотека — 10 эталонов (`app/exemplars/`), отчёт `results/quality-report.md` с PNG.

Провайдеры: без API-ключей сервер и воркер используют консольные аккаунты Codex CLI / Claude Code (`app/cli_llm.py`, маршрутизация в `llm_client.chat_envelope`); живой smoke vision-судьи через Codex CLI: эталон saas-landing → 72/100 за 28 с с адресными замечаниями. Полный отчёт: `.venv\Scripts\python tools/quality_report.py`, `--dry-run` без сети.

### Живой цикл через Codex CLI (2026-09-03, без API-ключей)

- Генерация полной страницы: 141 с (gpt-5.6-sol medium), результат — композиция с именованным направлением «Диспетчерская смена», реальная копия, один CTA, заглушка с арт-дирекцией (`results/live_generate_saas.png`).
- Vision-судья: 46/100 с адресными замечаниями; после LLM-починки 55 (`results/live_generate_saas_repaired.png`). Починка заменила заглушку «интерфейсом из примитивов» и внесла наложения во free-раскладке → добавлено правило `free-overlap` в `qualitygate`, рубрика перестала штрафовать заглушки с арт-дирекцией, починке запрещено их заменять.
- Следующие рычаги (по замечаниям судьи): верхний отступ hero-композиций (~150 px пустоты), масштаб заголовка hero (роль `display`, не `h1`), плотность первого экрана. Это правки промпта/эталонов (T3-зона), не архитектуры.

### Почему эталоны оценивались на 42–68 (найдено 2026-09-03)

Судья на каждой странице писал «ряды hero сложены в столбик, две колонки стоят одна под другой». Причины оказались в рендере и данных, не в промпте:

1. **Артборд 960 px.** IR без корневого `frame.width` рендерится на холсте `DESIGN_WIDTH = 960` и масштабируется 1.5× до 1440. Промпт (`DESIGN.md`) просит компоновать под 1440, модель даёт колонку 650 + кнопку в контейнере `wide` (1120), а реальный контейнер — 896 px (960 − 2×32). Всё, что не влезало, переносилось. Теперь `sanitize_generated_ir` ставит `frame.width = 1440` каждой генерации, все эталоны несут артборд 1440, `render_png` снимает IR без артборда на ширине вывода.
2. **Шрифты без кириллицы.** DM Serif Display, Space Grotesk, Fraunces, Newsreader в Google Fonts не имеют кириллицы — заголовки эталонов падали в Inter, судья писал «display-гарнитура не проявляется». Эталоны переведены на Prata / Unbounded / Manrope / Vollkorn / Playfair Display; пара `fraunces-newsreader` в `typography.py` заменена на `lora-literata`.
3. **Дети ряда без ширины.** Заглушка картинки и заголовок в auto-row брали max-content подписи/текста и выдавливали соседнюю колонку на новую строку. Рендерер даёт `image`/`heading`/`text` без явной ширины в row-раскладке `flex:1 1 0; min-width:0`.
4. **Судья видел одну длинную картинку** (1440×4400) и принимал её за мобильную вёрстку. `_judge_images` отдаёт первый экран 1:1 (1440×900) плюс до 4 плиток по 1400 px.
5. Навбар `minimal` не рисовал заданный `props.cta` — судья снимал за «предусмотренный, но невидимый CTA». Теперь рисует его outline-кнопкой.

Эталон `real-estate` тем же судьёй (Codex CLI, бриф совпадает с содержанием): 52 → 78 после правок 1–4 → **86, pass** после 5 и flex-заголовков (`results/real-estate-artboard.png`). Оставшиеся замечания — минорные (высота hero-кадра, кегль подписи заглушки, воздух финальной секции).

**Живой отчёт после правок (2026-09-03 17:00 UTC, Codex CLI, `results/quality-report-live.md`):**

| Эталон | до | после |
|---|---:|---:|
| ecommerce | 42 | 72 |
| education | 68 | 66 |
| fintech | 52 | 78 |
| marketplace | 62 | 72 |
| portfolio | 48 | 68 |
| real-estate | 42 | 89 |
| saas-landing | 64 | 68 |
| travel | 58 | 84 |
| healthcare / restaurant | 58 / 58 | сбой Playwright goto (гонка перехвата, добавлен повтор) |

Бенчмарк пяти брифов: after-оценки 84 / 58 / 62 / 72 / 48, медиана 62 (до правок 72 на 3 парах). Скриншоты `after` показывают следующий рычаг — модель компонует страницу одной колонкой 560–650px с пустым холстом справа и растягивает её до 6–8 тыс. px (fintech), а списки шагов заполняет пустыми карточками «Шаг 1/2/3» (marketplace). Добавлен раздел «Desktop width discipline» в `DESIGN.md`: ряды из 2–3 колонок на всю рейку 1120, лимит 6–8 секций и ~5500px, запрет пустых карточек.

**Прогон №3 (после «Desktop width discipline», 2026-09-03):** бенчмарк after 72 / 73 / 76 / 76 / 76, медиана **66 → 76**; эталоны 64–88 (fintech 84, real-estate 88, travel 88, restaurant 81). Все 10 эталонов оценены (повтор goto сработал). До цели (медиана ≥ 80, эталоны ≥ 90) остаются: saas-landing/education/portfolio 64–68 — следующий рычаг там.

### Точность Source Import → Design System и генератор с входом ДС (2026-09-03)

Разбор на живом захвате slsbmb.com (9 блоков, 15 review-компонентов с pixel similarity 84–94 < 95):

1. **«Текст дважды» в «Мастере из источника».** Мастер вырезался из блока без корневого `responsive.viewports`, рендерер не применял `visible:false` у клонов других вьюпортов — desktop- и mobile-версии строк рисовались друг поверх друга. Мастер теперь несёт маркер блока, превью синтезирует его с размерами компонента (`document.preview_ir_for_master`).
2. **Faux-bold подписей.** Сайт объявляет JetBrains Mono 400/500/600 поверх одного файла 400 (без синтеза жирности), а `_resolve_font_faces` резал faces лимитом 12 — 500/600 моно выпадали, рендер синтезировал жирность и каждая подпись расходилась по ширине. Лимит 48 с приоритетом используемых весов; для source-секций `font-synthesis:none`.
3. **Шрифты harness.** `_inline_font_face_css` искал `/fonts` только в `<repo>/data`; десктоп хранит их в `DESIGNDNA_DATA_DIR/fonts`. Теперь оба пути.
4. **Метрика.** Точное попиксельное сравнение считало расхождением каждую кромку глифа при сдвиге текста на 1px: блок с идеальной геометрией (bbox p95 1px, origin 0, coverage 99) получал 90%. `pixel_similarity` теперь с допуском сдвига 1px (как pixelmatch), точное значение — `pixel_similarity_exact`. Блоки slsbmb: pricing 94.8 → 97.8, section 92.4 → 95.2, section-3 96.6 → 98.9; компоненты 88–93 → 93–97.
5. **Остаток дрейфа** — вертикальные сдвиги строк на 2–3px внутри free-фреймов (округление line box / baseline), на mobile у gallery — иной перенос строк (78–91). Это следующий рычаг парсера, не метрики. Чтобы ДС пересчиталась по новым правилам, нужен повторный импорт источника («Обновить» на ноде Source Import).

Генератор с входом ДС: порт ноды отдавал плоскую shadcn-карту, сервер лочил из неё один ключ `radius` → «tokens: 'color' is a required property». Теперь любая форма приводится к полным tokens v1 (`coerce_ir_tokens`), ДС хранит `styleGuide.irTokens` и `styleGuide.profile` (тема, углы, плотность, тени, палитра, характер типографики, imageDirection, образцы копирайта из мастеров), компилятор ДС отдаёт `Style profile` обязательной строкой, генератор добавляет блок «Стиль и атмосфера исходного сайта».

Критерий приёмки всего проекта: на 5 фиксированных брифах (SaaS, маркетплейс, ресторан, портфолио, финтех) медианный балл vision-судьи ≥ 80 при старте с ~55 (оценка текущего сэмпла), ни одной страницы с слоп-тропами из рубрики, все тесты зелёные.
