Create one standalone visual asset for an editable web design.
Subject: {subject}
Requested image size: {width} x {height} pixels.
Visual context for this series: {context}
Use the attached image, when present, only as a reference for palette, materials,
lighting and illustration technique. Create the requested subject separately.
Do not draw interface elements, typography, labels, numbers, logos or watermarks.
Text and controls will be composed as live elements in the design editor.
Keep the main subject inside the image with room for cropping at narrow widths.
Transparent background required: {alpha}. When required, return a real alpha channel.
