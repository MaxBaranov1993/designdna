"""HTTP-роутеры DesignDNA по доменам.

`server.py` остаётся точкой сборки приложения и владельцем генератора,
разбора источника и Quality Pass; самостоятельные группы маршрутов живут
здесь и подключаются через `app.include_router`. Имена хендлеров и моделей
запросов, на которые ссылаются тесты, `server.py` реэкспортирует.
"""
